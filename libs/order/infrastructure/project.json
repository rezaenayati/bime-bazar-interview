{
  "name": "order-infrastructure",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/order/infrastructure/src",
  "projectType": "library",
  "tags": [
    "scope:order",
    "type:infrastructure"
  ],
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "jest --config libs/order/infrastructure/jest.config.js --passWithNoTests"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint libs/order/infrastructure/src"
      }
    }
  }
}
